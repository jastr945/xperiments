apiVersion: v1
kind: Service
metadata:
  name: kubebooks
  labels:
    name: kubebooks
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    name: kubebooks
