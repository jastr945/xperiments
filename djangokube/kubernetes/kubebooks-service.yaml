apiVersion: v1
kind: Service
metadata:
  name: kubebooks
  labels:
    name: kubebooks
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: kubebooks

# apiVersion: extensions/v1beta1
#   kind: Deployment
#   metadata:
#     name: kubebooks
#     labels:
#       name: kubebooks
#   spec:
#   replicas: 3
#   template:
#     metadata:
#       labels:
#         name: kubebooks
#     spec:
#       containers:
#         - name: kubeapp
#           image: jastr945/kubebooks:1.0
#           ports:
#             - containerPort: 80
